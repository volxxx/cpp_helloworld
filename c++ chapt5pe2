#include<iostream>

using namespace std;


int main()
{

    double a, sum;
    sum = 0.0;
    while (a!=0)
    {
       cout << "Please type in numbers: " << endl;
       cin >> a;
       sum = sum + a;
       cout << sum << endl;
    }
    return 0;
}
