#include<iostream>

using namespace std;


int main()
{
    int num1,num2,sum;
    cin >> num1;
    cin >> num2;
    sum = num1;
    for (int i=1;i<= num2 -num1;++i)
    {
        sum = sum + (num1+i);

    }
    cout << sum << endl;
    return 0;
}
